pub struct OakOptions {
    pub min_thickness: f32,
    pub max_length: f32,
    pub min_length: f32,
    pub max_angle: f32,
    pub start_thickness: f32,
    pub start_x_angle: f32,
    pub start_y_angle: f32,
}
